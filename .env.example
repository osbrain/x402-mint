# ===== RPC 配置 =====
# Base 主网 RPC 节点（可选择 Alchemy/Infura/LlamaRPC 等）
RPC_URL_BASE="https://base.llamarpc.com"

# ===== 部署者私钥 =====
# 用于部署合约的账户私钥（请使用临时账户，部署后转移余额）
DEPLOYER_PRIVATE_KEY="0x..."

# ===== 代币参数 =====
# 代币名称
TOKEN_NAME="LICODE"
# 代币符号
TOKEN_SYMBOL="LICODE"
# 总供应量（单位：个，脚本会自动转换为18位小数）
TOTAL_SUPPLY_18="1000000000"
# 兑换率：1 USDC 可兑换的代币数量（脚本会自动转换为18位小数）
TOKENS_PER_USDC_18="5000"

# ===== 限额配置（USDC，6位小数）=====
# 总USDC限额（100,000 USDC = 100000000000，6位小数）
TOTAL_USDC_CAP_6="100000000000"
# 单钱包USDC限额（10 USDC = 10000000，6位小数）
PER_WALLET_USDC_CAP_6="10000000"

# ===== 网络配置 =====
# 链 ID（8453 = Base 主网，84532 = Base Sepolia 测试网）
CHAIN_ID="8453"

# ===== 关键地址配置 =====
# Distributor 地址（后端服务账户，用于调用 distribute() 分发代币）
DISTRIBUTOR_ADDRESS="0x4eb111A2bdB7F8a4e0DDE5E099210a7408C50a20"
# Owner 地址（合约所有者，拥有管理权限）
OWNER_ADDRESS="0x7488966a774199BD475763D9f9Ea99F17a273652"

# ===== Basescan API 密钥（可选）=====
# 用于在 Basescan 上验证合约，可在 https://basescan.org/myapikey 获取
BASESCAN_API_KEY=""

# ===== 可选：已部署合约地址 =====
# 如果已部署，可以填写合约地址（用于 scripts/withdraw.ts 等）
TOKEN_ADDRESS=""

# ===== 可选：代币提取配置 =====
# 提取代币的目标地址（用于 scripts/withdraw.ts）
WITHDRAW_TO_ADDRESS=""
# 提取代币数量（整数，例如 1000000 表示 100 万个代币）
WITHDRAW_AMOUNT_18="0"
